#include <iostream>
#include "Text.h"
#include "TodoListArray.h"

int main() {
	int Text(answer)
  while (answer == 'y') {
		int TodoListArray(array[x])
    std::cout << "Do you want start again?\n(Y/n): ";
		std::cin >> answer;
	}
}
