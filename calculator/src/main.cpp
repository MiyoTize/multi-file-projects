#include <iostream>
#include "Text.h"
#include "BasedMath.h"

int main() {
  int z;
  Text(int answer);
  std::cout << "Start?\n(Y/n): ";
  std::cin >> z;
  while (z == 'y') {
    if (answer = 1) {
			BasedMath(double x, double y);
    }
    std::cout << "Do you want solve something again?\n(Y/n): ";
    std::cin >> z;
	}
  return 0;
}
